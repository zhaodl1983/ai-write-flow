# image-config.md

Deprecated. The default article illustration workflow uses `illustration-config.md` and the local svg-architect skill.

External image API generation is not part of the default flow. This file is retained for reference only and should not be loaded by the skill.

